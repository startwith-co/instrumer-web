import RegisterForm from './components/register-form';

const RegisterPage = () => {
  return (
    <div className="flex min-h-[calc(100vh-60px)] items-center justify-center py-8">
      <RegisterForm />
    </div>
  );
};

export default RegisterPage;
